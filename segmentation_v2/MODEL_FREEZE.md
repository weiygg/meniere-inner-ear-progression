# Model freeze

Status: **NOT FROZEN**

Reason: formal patient-level five-fold OOF experiments are incomplete. No V2
architecture, loss, augmentation, checkpoint, postprocessing or TTA configuration
has been selected.

External evaluation is not authorised by this file. When the internal winner is
selected, this document must record the full immutable configuration and include:

> Model frozen before external evaluation. No external annotation was used for
> model selection.

The existing 50-patient manual-reference set is already exposed and cannot become a
new confirmatory external cohort.
