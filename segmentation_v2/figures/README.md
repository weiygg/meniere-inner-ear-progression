# Segmentation V2 figures

Only aggregate, de-identified figures may be committed here. Patient-derived
overlays remain under the ignored local `segmentation_v2/results/failure_cases_*`
directories and must never be uploaded.
