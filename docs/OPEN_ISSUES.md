# Open issues

No issue below may be silently guessed. The affected-record counts are audit counts,
not final cohort denominators.

| ID | Issue | Affected material / records | Possible solutions | Recommended decision |
|---|---|---|---|---|
| O001 | Clinical source provenance and PHI | whole protected clinical workbook; direct identifiers in all three sheets | obtain source-owner statement, export history, and a de-identified working copy generated by script | keep workbook read-only and outside Git; approve it as canonical only after provenance review |
| O002 | Sheet 2 to sheet 3 patient/visit linkage | 112 patient-level rows vs 103 paired-ear visits | provide `浙二MD.编号 -> 浙二.ID` mapping and visit-date rule | require an explicit crosswalk; do not join on names, row order, or approximate dates |
| O003 | Site-local ID collision and segmentation linkage | prior audit found 29 numeric IDs shared across sites; segmentation folders use local numeric IDs | provide `seg_subject_id + source_site -> source_subject_id` mapping with ear side | namespace every persistent key by site and source before any merge |
| O004 | Exact hearing-feature rule, bilateral cases, and first affected/index ear (partially resolved) | affected-ear status is confirmed to come from abnormal clinical hearing features; an aggregate 30-rule x 5-mode sensitivity audit is recorded in `reports/01_hearing_ear_rule_sensitivity.md`, but it does not establish clinical correctness | specify the hearing features, abnormal thresholds, test-date rule, and handling when neither or both ears meet the rule; add `first_affected_ear` and bilateral status where available | use the sensitivity audit to quantify consequences, then freeze the clinical rule prospectively; do not use AAO-HNS-stage presence or select the numerically worse ear from yield alone |
| O005 | PTA and AAO-HNS stage provenance | 112 LS and 194 Z2 complete four-frequency ear rows; stage present in only subsets | confirm dB HL units, examination timing, masking/censoring rules, and stage derivation; rebuild in code | calculate `PTA_AAOHNS_0.5_1_2_3k` reproducibly; do not create a 4-kHz PTA without source 4-kHz data |
| O006 | Clinical variable codebook | CochEH, VestEH, VA, ES/ED, sex, ear fullness, symptom scores, vestibular testing | supply allowed values, units, missing codes, abnormal direction, acquisition method, and thresholds | freeze a signed variable dictionary before role or event-state assignment |
| O007 | Normal/abnormal mixture reference groups | all prospective P-EBM biomarkers | define truly appropriate controls/abnormal labels or supported unsupervised mixture assumptions | never label a symptomatic contralateral ear as healthy by default |
| O008 | Canonical segmentation batch and label map | `xjj内耳分割`, `xjj内耳分割2`, `seg3`, `seg4`, and archive extraction; duplicate subjects/ears | provide a patient-level version manifest and standardized label dictionary | select one authoritative mask per patient/ear/structure; do not use modification date alone |
| O009 | Structure names and image orientation | `Chochlear` vs `Cholear`; ELS/TV/Vestibular meanings; left-right surface comparisons | confirm label semantics, DICOM/NIfTI orientation, and registration/reflection protocol | postpone endotype/surface asymmetry features until the map and geometry protocol are frozen |
| O010 | Baseline and longitudinal visit definition | 100 Z2 baseline people plus 3 identified follow-up visits; sheet 2 contains dates | confirm index assessment, treatment timing, allowable visit window, and outcome landmarks | create visit keys and prediction landmarks before any longitudinal validation |
| O011 | Biomarker compatibility with official mixture models | ordinal hydrops/VA/ES-ED, binary symptoms, continuous audiometry; official code supports specific distributions | inspect supported KDE/GMM pathways and run eligibility diagnostics by variable type | exclude or separately model incompatible ordinal/binary variables; do not coerce them to unjustified continuous events |
| O012 | Non-monotonic symptom variables in prior P-EBM | DHI-T, THI, ear fullness, VADL in the 96-patient pilot | reclassify as validators/outcomes unless longitudinal monotonicity supports otherwise | do not include them in the primary irreversible event sequence at present |
| O013 | Static endotype markers | ATVA/VA morphology and derived anatomy | define published/end-project criteria and reliability; keep separate from progression events | use for endotype/stratification/effect modification, not primary progression events |
| O014 | Legacy outputs conflict | two obsolete ear-level pilots plus one nonconforming patient-level pilot | retain hashes, freeze as legacy, rerun only after all gates pass | prohibit copying their event order, stage, AUC, or p-values into new outputs |
| O015 | Incomplete implementation | 12 phase stubs; no canonical package runner or per-run manifest | build package modules, tests, config schema, and `scripts/run_pipeline.py` phase gates | implement incrementally after this checkpoint; every run must be isolated and manifested |
| O016 | Official P-EBM environment cleanliness | upstream checkout pinned to `ffbe8a9` but contains untracked bytecode caches and an upstream module-name test incompatibility | create a clean vendor checkout/cache outside results and document environment lock | rerun the official walkthrough in a clean environment before any new clinical fit |

## Information requested from the study team

1. Clinical codebook with units, direction, missing codes, and thresholds.
2. Site/patient/ear/visit crosswalk, including sheet 2-to-3 mapping.
3. Exact hearing features and thresholds defining an abnormal ear, plus the rule for
   bilateral abnormal hearing and first affected/index ear.
4. Authoritative segmentation-version and structure-label map.
5. Written confirmation that the three previous clinical P-EBM runs are audit-only
   and may be moved to `archive/legacy/` in a later change.
